extends layout

block css
    link(rel='stylesheet', href='/stylesheets/index.css')

block content
    h1 #{title}
    br

    if #{reqType} == "read":
        textarea(readonly) #{content}
    else
        textarea #{content}