extends layout

block css
    link(rel="stylesheet", href="/stylesheets/index.css")
    link(rel="stylesheet", href="/stylesheets/search_result.css")

block content
    .wrap
        each idx, file in files
            .card
                a(href="#", onclick="onDocumentClicked('#{file}')") #{idx}
